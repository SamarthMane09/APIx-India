/**
 * Vercel Serverless Function Entry Point for APIx-India
 * Automatically handles all /api/* routes on Vercel with zero configuration.
 */

import app from '../src/serverApp';

export default app;
